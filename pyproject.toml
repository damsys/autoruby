[project]
name = "autoruby"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "janome>=0.5.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.4",
]
